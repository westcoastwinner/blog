# 计算机相关知识结算

## 网络

### HTTPs

HTTPs=HTTP+SSL/TLS, 其实是在应用层和传输层之间加了一层，可以理解为HTTP安全版（加密传输）。

在此之前，把握非对称加密两个凡是：

- 凡是公钥加密的，只能用对应私钥解密；
- 凡是私钥加密的，只能用对应公钥验证。

HTTPs正是同时利用了非对称加密和对称加密来完成信息传输的，具体过程如下：

在建立TCP连接后，进行TLS握手，这个过程完成**客户端对服务器信任的建立**和**客户端与服务器对称密钥的确定**

所谓客户端对服务器信任的建立，即要求服务器证明本人是本人。这是通过**数字证书**进行的。

所谓数字证书，即包含持有者身份信息和持有者公钥的一份证书，由CA机构颁发，需要持有者进行申请。CA 用自己的**私钥**对申请者的信息和公钥进行数字签名，生成最终的 数字证书。浏览器内置CA的公钥，从而可以验证来自服务器的数字证书。



TLS握手完整过程如下：

**第一步：Client Hello（客户端 -> 服务器）**

客户端（浏览器）先打招呼，并送上一份“菜单”：支持的 TLS 版本（如 TLS 1.2）、支持的加密算法组合，以及**客户端随机数 (Client Random)**，这是后续生成密钥的种子之一。

**第二步：Server Hello & 证书发送（服务器 -> 客户端）**

服务器从菜单里选好后回应：确认 TLS 版本和加密套件，**服务器随机数 (Server Random)**：第二个种子，**服务器数字证书**：包含服务器的公钥和身份信息。

**第三步：证书验证 & 密钥交换（客户端 -> 服务器）**

客户端收到证书后，先通过浏览器内置的 CA 公钥验证证书合法性。如果没问题：

**生成 Pre-Master Secret**：客户端再生成一个随机数，并用证书里的**服务器公钥**加密。

**第四步：握手完成（服务器 -> 客户端）**

服务器用私钥解开包，得到 `Pre-Master Secret`。

现在双方都有了 `Client Random` + `Server Random` + `Pre-Master`。三者结合生成**对话密钥（Session Key）**，从此双方通信用此对称密钥。

**Finished**：服务器也发一个加密消息给客户端，确认握手圆满成功。

### HTTP1.1 v.s. HTTP2

 HTTP2核心优势：

1. **二进制分帧 (Binary Framing)** ，这是 HTTP/2 与 HTTP/1.1 最根本的区别。HTTP/2将所有传输信息分割为更小的**帧 (Frame)**，并采用二进制编码。这使得协议解析更高效、更紧凑。（**原理**：每个数据包开头都有固定的 9 个字节（帧头），里面明确写着：这个帧有多长、是什么类型（是 Header 还是 Data）、属于哪个流（Stream ID）。**优势**：由于格式固定，计算机直接按位读取即可。就像读一个 C 语言的 `struct` 或 Java 对象，不需要去搜寻换行符。这种强结构化的设计才是所谓的“二进制编码”优势所在

------

**2. 多路复用 (Multiplexing) —— 解决队头阻塞**

这是 HTTP/2 提升速度最明显的杀手锏。

- **HTTP/1.1 的困境**：虽然有 Pipelining，但必须按顺序响应。如果前面的请求卡住了（比如一个大图片加载慢），后面的请求就得排队，这就是 **队头阻塞 (Head-of-Line Blocking)**。

- **HTTP/2 的解法**：在同一个 TCP 连接上，可以**并行**发送多个请求和响应。每个流 (Stream) 都有唯一标识，数据包可以交错传输，接收端再根据 ID 重组。

  > **直观对比**：HTTP/1.1 像是只有一条车道的单行线；HTTP/2 则像是一条多车道高速公路，小车（小文件）可以直接超车大货车（大资源）。

------

**3. 头部压缩 (HPACK) —— 极致减重**

- **问题**：HTTP 是无状态的，每次请求都要带上 User-Agent、Cookie 等冗长的 Header。对于频繁的小请求，Header 的大小甚至超过了 Body。
- **HTTP/2 的方案**：引入了 **HPACK 算法**。
  - **静态/动态表**：双方共同维护一张表，频繁出现的 Header 只传索引（例如用数字 `2` 代表 `GET`）。
  - **Huffman 编码**：对字符串进行压缩传输。
  - **效果**：Header 占用带宽通常能减少 **80% 以上**。

------

**4. 服务端推送 (Server Push)**

- **HTTP/1.1**：浏览器解析完 HTML，发现需要 CSS 和 JS，才再次发请求。
- **HTTP/2**：服务器可以“预判”你的需求。当你请求 `index.html` 时，服务器在响应 HTML 的同时，主动把 `style.css` 推送给浏览器。这样当浏览器解析到需要 CSS 时，它已经躺在缓存里了，减少了一次往返延迟。

## 系统